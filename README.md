# 📚 Rust Book Parser

**parser** — асинхронный Rust-инструмент для агрегации и парсинга данных о книгах из открытых sitemap и HTML страниц интернет-магазинов:
- [Labirint](https://www.labirint.ru)
- [Игра Слов](https://igraslov.store)
- [Эксмо](https://eksmo.ru)

Собирает ISBN, авторов, название и источник, сохраняя результат в `books.csv`.

## 🚀 Возможности
- Параллельный парсинг (управляется параметром `<at_once>`)
- Ограничение числа книг с каждого сайта (`<how_much_from_one_store>`)
- Идиоматичный асинхронный код на `Tokio`
- Логирование в консоль и файл (`logs/parser.log`) через `tracing`
- Автоматическая приоритизация ISBN-13 если найдено несколько
- Генерация итогового CSV с гибкой структурой

## 🧱 Архитектура
Модули:
- `main.rs` — orchestration: загрузка sitemap, интерливинг URL, конкурентный парсинг.
- `parse_traits.rs` — обобщённые трейты и структуры `Book`, `BookParser`.
- `labirint.rs`, `igraslov.rs`, `eksmo.rs` — реализации парсеров сайтов.
- `csv_save.rs` — сохранение в CSV.
- `telemetry.rs` — настройка `tracing` с раздельными слоями времени.

Парсеры реализуют трейт `BookParser`, что упрощает расширение (добавьте новый модуль и включите в агрегирование URL).

## 📦 Зависимости
Из `Cargo.toml`:
- `anyhow` — удобные ошибки
- `quick-xml` — парсинг XML sitemap
- `reqwest` — HTTP клиент
- `scraper` — HTML парсинг
- `serde` — десериализация XML структур
- `tokio` — асинхронный runtime
- `tracing`, `tracing-subscriber`, `tracing-appender` — продвинутое логирование
- `csv` — запись результатов
- `futures` — stream / concurrency утилиты

## 🛠 Сборка и запуск
Требования:
- Rust 1.81+ (edition 2024)
- Интернет доступ к целевым сайтам

Установка:
```bash
rustup update stable
cargo build --release
```

Запуск:
```bash
# Формат: parser <at_once> <how_much_from_one_store>
# Оба параметра опциональны.
./target/release/parser 5 800
```
Если параметры не указаны, используются значения по умолчанию:
- `<at_once>` = 3
- `<how_much_from_one_store>` = 1500

## 🗂 Выходные данные
Файл `books.csv` имеет заголовки:
```
site,source,isbn,title,authors
```
Пример строки:
```
labirint,https://www.labirint.ru/books/12345/,9785000000000,"Название книги","Автор Один; Автор Два"
```

## 🧪 Расширение проекта
Чтобы добавить новый сайт:
1. Создайте модуль `your_site.rs`.
2. Реализуйте трейт `BookParser` (fetch + parse_* методы).
3. Добавьте получение sitemap/URL в `main.rs` и включите интерливинг.
4. Пересоберите и запустите.

## 🔍 Логирование
- Файл: `logs/parser.log` (ротация по дням)
- В консоли время выводится только для целевого `target = "time"`
- Используйте переменную окружения `RUST_LOG` для фильтра: `RUST_LOG=debug ./parser`

## ⚙️ Производительность
- `buffer_unordered` обеспечивает максимальное использование параллелизма
- Минимизация аллокаций при интерливинге URL
- Предпочтение `&str` и borrowing вместо лишних `String`

## 🛡 Безопасность и надежность
- Проверка и нормализация аргументов CLI
- Явная обработка ошибок HTTP (`error_for_status`) для igraslov
- Валидация ISBN длиной и форматом
- Структурированное логирование успешных и неуспешных парсов

## ❗ Потенциальные улучшения
- Добавить retry/backoff для сетевых запросов
- Кэшировать уже обработанные URL
- Писать CSV построчно (flush после каждой записи) для минимизации потерь
- Разделить ошибки по типам (custom error enum)
- Добавить тесты модулей парсеров (unit + integration)

## 🤝 Вклад
PR и предложения приветствуются. Соблюдайте чистоту кода, избегайте `unwrap()` и паник без необходимости.

## 📄 Лицензия
Укажите лицензию проекта (например, MIT) в нужном разделе.

---
_Сгенерировано 2025-11-09._
